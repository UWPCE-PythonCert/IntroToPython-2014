#!/usr/bin/python

#list = [1,2,3]
#
#function = [lambda x: i += for i in list]
#
#
#print function[0](list[0])
user_input = int(raw_input('Int: '))
function = lambda x: x+x

for i in user_input:
    function(x)

print function()
